// This is your main entry point - use this for local development
// For Vercel, you'll use server.js as the serverless function

import { server } from "./socketServer.js";

// The server is already started in socketServer.js
// This file just imports it to trigger the startup