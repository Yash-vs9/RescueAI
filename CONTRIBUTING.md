# Contributing Guide

> Auto-generated by [Phantom DX Scanner](https://phantom-dx.vercel.app) — DX Score: 72/100

## Getting Started

```bash
git clone <repo-url>
cd RescueAI
npm install
cp .env.example .env   # fill in your values
npm run dev
```

## Issues Fixed by This PR

- **Missing environment variable documentation** (high): No .env.example file is present, making it difficult for new developers to understand required environment variables.
- **Lack of test coverage** (high): No tests are present in the project, making it difficult to ensure code quality and catch regressions.

## Quick Wins (do these manually)

- **Add a .env.example file** (5min): Create a .env.example file with required environment variables, e.g., `touch .env.example` and add variables like `DB_HOST=localhost`
- **Organize code into subfolders** (30min): Create subfolders for related files, e.g., `mkdir controllers/auth` and move related files into it

---
*Generated by Phantom DX Scanner*
