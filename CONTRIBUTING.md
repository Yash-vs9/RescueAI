# Contributing Guide

> Auto-generated by [Phantom DX Scanner](https://phantom-dx.vercel.app) — DX Score: 72/100

## Getting Started

```bash
git clone <repo-url>
cd RescueAI
npm install
cp .env.example .env   # fill in your values
npm run dev
```

## Issues Fixed by This PR

- **Missing environment variable documentation** (high): No .env.example file is present to document required environment variables.
- **Insufficient test coverage** (high): No test results are available, and no test scripts are defined in package.json.

## Quick Wins (do these manually)

- **Add a .env.example file** (5min): Create a .env.example file with required environment variables, e.g., `touch .env.example` and add variables like `DB_HOST=localhost`
- **Define test scripts in package.json** (5min): Add test scripts to package.json, e.g., `npm test` or `jest`, and write unit tests for critical components.

---
*Generated by Phantom DX Scanner*
