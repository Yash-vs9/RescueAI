# Contributing Guide

> Auto-generated by [Phantom DX Scanner](https://phantom-dx.vercel.app) — DX Score: 70/100

## Getting Started

```bash
git clone <repo-url>
cd RescueAI
npm install
cp .env.example .env   # fill in your values
npm run dev
```

## Issues Fixed by This PR

- **Missing .env.example file** (high): The project does not provide an example environment file, making it difficult for new developers to set up the project.
- **Insufficient test coverage** (high): The project does not have a comprehensive test suite, making it difficult to ensure the code works as expected.

## Quick Wins (do these manually)

- **Add a .env.example file** (5min): Create a .env.example file with required environment variables, e.g., `touch .env.example` and add variables like `DB_HOST=localhost`
- **Improve README documentation** (30min): Add more detailed information about the project, its dependencies, and how to set it up, e.g., `npm install` and `node server.js`

---
*Generated by Phantom DX Scanner*
